# Web server
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart  # take uploads

# YouTube
# yt-dlp and its optional helpers, without mutagen (GPL, only used for tagging files).
# Pinned: each release ships a yt-dlp version it was tested with. Bump deliberately.
yt-dlp==2026.8.19
yt-dlp-ejs==0.8.0
brotli
certifi
pycryptodomex
requests
urllib3
websockets

# Audio I/O and click rendering
numpy
soundfile
mido
scipy  # electronic-style cleanup, take resampling and video sync

# Separation (pulls in torch)
demucs==4.0.1

# Beat tracking
beat_this @ https://github.com/CPJKU/beat_this/archive/main.zip
